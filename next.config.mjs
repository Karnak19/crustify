/** @type {import('next').NextConfig} */
const nextConfig = {
	// images: {
	// loader:'custom',
	// 	remotePatterns: [
	// 		{
	// 			hostname: "api.dicebear.com",
	// 		},
	// 	],
	// },
};

export default nextConfig;
